/**
 * @deprecated Replaced by SideNav + TabBar + MobileHeader (design-reference shell).
 * Kept so any stray imports don't break the build; prefer the new components.
 */
export { SideNav as NavBar } from './SideNav';
